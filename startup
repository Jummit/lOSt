local processAPI = require "apis.processes"

local processes = {}

while true do
  -- update
  local event, var1, var2, var3 = os.pullEventRaw()
  processAPI.update(processes, event, var1, var2, var3)

  -- draw
  processAPI.draw(processes)
end
